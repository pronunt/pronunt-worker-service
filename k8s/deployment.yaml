apiVersion: apps/v1
kind: Deployment
metadata:
  name: pronunt-worker-service
  namespace: pronunt-app-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pronunt-worker-service
  template:
    metadata:
      labels:
        app: pronunt-worker-service
    spec:
      containers:
        - name: pronunt-worker-service
          image: ghcr.io/pronunt/pronunt-worker-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: pronunt-worker-service-config
            - secretRef:
                name: pronunt-worker-service-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20


